# Zing Web — environment example
# Copy to .env.local for local dev. On Railway, set these in the Frontend service variables.

# Backend API URL. Local: http://localhost:5000
# Railway: use your Backend service URL (e.g. https://your-backend.up.railway.app)
NEXT_PUBLIC_API_URL=http://localhost:5000
