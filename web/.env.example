# Zing Web — environment example
# Copy to .env.local for local dev. On Railway, set these in the Frontend service variables.

# Backend API URL (no trailing slash).
# Local: http://localhost:5000
# Railway: set in Frontend service Variables, e.g. https://zestful-laughter-production.up.railway.app
NEXT_PUBLIC_API_URL=http://localhost:5000
